<h1>Editing <%= @song.name %></h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, value: @song.title %>
  <%= f.label :artist %>
  <%= f.text_field :artist_id, value: @song.artist_id %>
  <br />

  <%= f.submit %>
<% end %>
